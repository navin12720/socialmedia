extends layouts/main-layout.pug 
block content
    +navbar(UserDetails)
    .container
        if !ProfileUser
            span Invalid Username or Check the url.
        else    
            .profile-head 
                .profile-container 
                    .pic 
                .profile-button-container 
                    if ProfileUser._id!=UserDetails._id 
                        a.profile-btn(href=`/messages/${ProfileUser._id}`)
                            ion-icon(name="mail")       
                    else          
                        a.profile-btn(href=`/messages/${ProfileUser._id}`)
                            ion-icon(name="mail") 
        
        
